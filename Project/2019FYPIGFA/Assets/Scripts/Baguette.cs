using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Baguette : Weapon
{
    // Start is called before the first frame update
    void Start()
    {
        weaponType = TYPE.CLOSE_RANGE;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
